/**
 * Stub for react-native's codegenNativeComponent.
 * On web (react-native-web) there is no native component codegen infrastructure,
 * so we export a no-op that satisfies the import without doing anything.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const codegenNativeComponent = (_name: string, _options?: any) => null;

export default codegenNativeComponent;
